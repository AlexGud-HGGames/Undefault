# Manual intent timeline

**Archived.** Hotkeys and `POST /user-actions` were removed (UND-78). The 2026-06-28 `--mvp` observe+record slice is no longer the product MVP ([product-pivot-2026-08-14.md](product-pivot-2026-08-14.md)).

Historical text: [archive/manual-intent-timeline.md](archive/manual-intent-timeline.md).

What still exists in code: `intent_capture` runtime, `GET /timeline`, `PlaybackStateObserver`. That is tester tooling, not the Tauon automation path. Timeline details for the current host are in [backend-architecture.md](backend-architecture.md).
